# Database phase

This directory is reserved for the MySQL Prisma schema and seed script in Phase 2.
Phase 1 has no database dependency, migrations, seed data or demo accounts.
The health endpoint checks API liveness only; it does not assert database readiness.
